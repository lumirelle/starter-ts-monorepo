amends "package://github.com/jdx/hk/releases/download/v1.50.0/hk@1.50.0#/Config.pkl"

import "package://github.com/jdx/hk/releases/download/v1.50.0/hk@1.50.0#/Builtins.pkl"

local linters = new Mapping<String, Step> {
  // Code
  ["oxlint"] = Builtins.ox_lint
  ["eslint"] = (Builtins.eslint) {
    batch = false
    glob =
      List(
        // Code
        "*.js",
        "*.mjs",
        "*.cjs",
        "*.jsx",
        "*.ts",
        "*.mts",
        "*.cts",
        "*.tsx",
        // Configurations
        "*.json",
        "*.jsonc",
        "*.yaml",
        "*.yml",
        "*.toml",
        // Documentations
        "*.md",
        "*.mdc",
        "*.mdx",
      )
    depends = List("oxlint")
  }
  ["tsc"] = Builtins.tsc
  // Unused
  ["knip"] = (Builtins.knip) {
    // Use root knip config
    workspace_indicator = null
    check = new Config.Command {
      argv = List("knip", "--no-progress")
    }
    // Disable knip auto-fix, because it's dangerous
    fix = null
  }

  // Configurations
  ["stylua"] = (Builtins.stylua) {
    glob = List("**/*.lua")
  }
  ["hk"] {
    glob = List("hk.pkl")
    check = new Config.Command {
      argv = List("hk", "validate")
    }
  }
  ["pkl-format"] {
    glob = List("hk.pkl")
    check_list_files = new Config.Command {
      argv = List("pkl", "format", "--diff-name-only", "{{ files }}")
    }
    fix = new Config.Command {
      argv = List("pkl", "format", "--write", "{{ files }}")
    }
  }
  // Publish configurations
  ["attw"] {
    glob = List("packages/**/*")
    workspace_indicator = "package.json"
    check = "cd {{workspace}} && attw --pack --profile esm-only"
  }
  ["publint"] {
    glob = List("packages/**/*")
    workspace_indicator = "package.json"
    check = "cd {{workspace}} && publint --pack npm"
  }
}

hooks {
  ["pre-commit"] {
    fix = true
    stash = "git"
    steps = linters
  }
  ["fix"] {
    fix = true
    steps = linters
  }
  ["check"] {
    steps = linters
  }
}
